Performance and UI flow improvements

- Sped up Part No selection by optimizing debounce (150ms) and adding instant local filtering to SearchableSelect while async DB fetch completes
- Enabled ArrowDown / ArrowUp keyboard navigation and Enter-to-next for Air Lead Time field
- PDF Print: Changed the default "Save as PDF" filename to automatically include the Customer's name and Quotation number
- Dashboard: Added console diagnostics to compare DB true count vs actual loaded count to explain missing sequence IDs
